<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <div class="container">
            <a href="/" class="logo">Bauteil-Konfigurator</a>
        </div>
    </header>

    <main class="container">
        <div class="error-page">
            <h1><%= title %></h1>
            <p class="error-message"><%= message %></p>

            <% if (details) { %>
            <div class="error-details">
                <h3>Details</h3>
                <pre><%= details %></pre>
            </div>
            <% } %>

            <div class="back-link">
                <a href="/">&larr; Zurück zur Startseite</a>
            </div>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>Lokales Tool zur Bauteil-Varianten-Auswahl</p>
        </div>
    </footer>
</body>
</html>
